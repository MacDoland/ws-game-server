import React from 'react';
import MainMenu from '../MainMenu';
import './Home.scss';

const Home = () => {
    return (
        <section className="home">
            <MainMenu />
        </section>
    )
}

export default Home;